.cards
	.col-sm-6
		.card-container
			.side
				= text_area_tag(:front, @card.front, disabled: true)

	.col-sm-4
		.answer
			%p.bold Your answer
			= form_tag(topic_card_back_path, method: 'get') do
				= text_area_tag(:u_answer, '', {placeholder: 'Your answer', class: 'form-control'})
				= text_field_tag(:start_time, @start_time, style: 'display:none')
				
				.result
					= submit_tag('Flip', class: 'btn btn-default')