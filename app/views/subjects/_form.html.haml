= form_for @subject do |f|
  - if @subject.errors.any?
    #error_explanation
      %h2= "#{pluralize(@subject.errors.count, "error")} prohibited this subject from being saved:"
      %ul
        - @subject.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :code
    = f.text_field :code, placeholder: "e.g. COM381, PS200"
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :color
    = f.text_field :color, placeholder: "e.g. 000000, ddd"
  .actions
    %a{href: "#{subjects_path}"} Cancel
    - if @url == new_subject_path
      = f.submit 'Create'
    - else
      = f.submit 'Save'