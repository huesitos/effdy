- if @topic.subject
	%p
		= link_to 'Subject', @topic.subject

= link_to 'New card', new_topic_card_path(@topic)

- if @exist_cards_to_study
	= link_to 'Study', topic_review_path(@topic)

.cards
	%p= "# of cards: #{Integer(@topic.cards.count)}"
	= render @topic.cards